# Copy to api.env (not committed). Used by deploy/compose/compose.prod.yml.
SPEEDIFY_STATUS_API_HOST=0.0.0.0
SPEEDIFY_STATUS_API_PORT=4090
# Optional shared secret for agent ingest (match INGEST_TOKEN on gw0)
# SPEEDIFY_STATUS_INGEST_TOKEN=change-me
